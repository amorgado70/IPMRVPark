@model IEnumerable<IPMRVPark.Models.countrycode>

@{
    ViewBag.Title = "Country Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2 class="margin-top-10">Country Index</h2>

<div class="container">
    <div class="row">
        @foreach (var item in Model)
        {
                        <div>
                            <h3>Code: @Html.DisplayFor(modelItem => item.code) Name: @Html.DisplayFor(modelItem => item.name)</h3>
                            <a href="@Url.Action("Details", "Country", new { id = item.code })" class="btn btn-primary margin-top-10">
                                Details
                                <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
                            </a>
                            <a href="@Url.Action("Edit", "Country", new { id = item.code })" class="btn btn-primary margin-top-10">
                                Edit&nbsp;
                                <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                            </a>
                            <a href="@Url.Action("Delete", "Country", new { id = item.code })" class="btn btn-primary margin-top-10">
                                Remove
                                <span class="glyphicon glyphicon-remove" aria-hidden="true"></span>
                            </a>
                        </div>
        }
    </div>
</div>
